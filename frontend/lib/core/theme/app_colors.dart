import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const primary = Color(0xFF6C63FF);

  // Financial
  static const positive = Color(0xFF22C55E);
  static const negative = Color(0xFFEF4444);

  // Neutral
  static const settled = Colors.white;
}